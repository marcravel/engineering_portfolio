---
import About from '../../components/pages/About.astro';

export function getStaticPaths() {
    return [
        { params: { lang: 'fr' } },
        { params: { lang: 'tr' } }
    ];
}

const { lang } = Astro.params;
---
<About lang={lang} />
